# Implementation Plan

- [x] 1. Write bug condition exploration test
  - **Property 1: Bug Condition** - Video Frames Render to Canvas
  - **CRITICAL**: This test MUST FAIL on unfixed code - failure confirms the bug exists
  - **DO NOT attempt to fix the test or the code when it fails**
  - **NOTE**: This test encodes the expected behavior - it will validate the fix when it passes after implementation
  - **GOAL**: Surface counterexamples that demonstrate the bug exists (canvas shows black instead of video content)
  - **Scoped PBT Approach**: Scope the property to concrete failing cases - video clip at playhead position with loaded video element (readyState >= 2, valid dimensions)
  - Test that for any timeline state where a video clip exists at the playhead position and the video element is loaded with valid dimensions, the canvas displays video content (not all black pixels)
  - Create test cases:
    - Basic video render: Import 1920x1080 video, position playhead at 2.5s, call renderFrame(), capture canvas output, verify NOT all black pixels
    - Video readiness state: Track video element readyState during load/seek, verify readyState >= 2 but canvas shows black
    - DrawImage direct test: Load video element, seek, wait for seeked event, call ctx.drawImage() directly, verify it draws content
    - Multiple clips: Import multiple video clips on different tracks, verify all show black instead of video content
  - The test assertions should match the Expected Behavior Properties from design: canvas contains non-black pixels representing video content
  - Run test on UNFIXED code
  - **EXPECTED OUTCOME**: Test FAILS (this is correct - it proves the bug exists)
  - Document counterexamples found:
    - Canvas ImageData shows all pixels are rgba(0,0,0,255) after renderFrame() completes
    - Video element reports readyState >= 2 but drawImage() produces no visible output
    - Possible root causes: video frame not decoded despite readyState, canvas context state issue, silent drawImage failure, video element not in drawable state
  - Mark task complete when test is written, run, and failure is documented
  - _Requirements: 1.1, 1.2, 1.3, 2.1, 2.2, 2.3_

- [ ] 2. Write preservation property tests (BEFORE implementing fix)
  - **Property 2: Preservation** - Non-Video-Rendering Behavior
  - **IMPORTANT**: Follow observation-first methodology
  - Observe behavior on UNFIXED code for non-buggy inputs (operations NOT involving video frame rendering)
  - Write property-based tests capturing observed behavior patterns from Preservation Requirements:
    - Canvas dimension calculation: Verify canvas dimensions are calculated correctly based on video aspect ratio (observe on unfixed code, should pass)
    - Loading state display: Verify "Loading preview...", "Seeking...", "No clips at this position" messages display correctly (observe on unfixed code, should pass)
    - Error handling: Verify video load errors display error messages correctly (observe on unfixed code, should pass)
    - Frame cache operations: Verify frame cache stores and retrieves frames correctly (observe on unfixed code, should pass even though frames are black)
    - VideoPool management: Verify VideoPool loads videos, manages reference counts, evicts LRU entries correctly (observe on unfixed code, should pass)
    - SeekManager logic: Verify SeekManager debounces seeks, respects threshold, handles timeouts correctly (observe on unfixed code, should pass)
  - Property-based testing generates many test cases for stronger guarantees
  - Run tests on UNFIXED code
  - **EXPECTED OUTCOME**: Tests PASS (this confirms baseline behavior to preserve)
  - Mark task complete when tests are written, run, and passing on unfixed code
  - _Requirements: 3.1, 3.2, 3.3, 3.4, 3.5_

- [x] 3. Fix for canvas video not showing
  - [x] 3.1 Implement the fix in RenderEngine.ts
    - Enhance video readiness check in `drawClipFrame()`:
      - Change readyState check from `< 2` to verify `readyState >= 3` (HAVE_FUTURE_DATA) or `readyState === 4` (HAVE_ENOUGH_DATA)
      - Ensure video frame is decoded and available for drawing, not just metadata loaded
    - Add video element validation before drawImage():
      - Validate video element has valid `currentTime`
      - Verify video is not paused in an invalid state
      - Check video has valid `src` attribute
      - Verify `videoWidth > 0` and `videoHeight > 0`
    - Enhance error logging for drawImage():
      - Add detailed logging in try-catch around `drawImage()`
      - Log video element state: readyState, currentTime, videoWidth, videoHeight, src
      - Log canvas context state: dimensions, transforms, globalAlpha
      - Log any browser-specific errors
    - Add diagnostic logging to track video element state when drawClipFrame() is called
    - _Bug_Condition: isBugCondition(input) where activeClips.length > 0 AND activeClips[0].type == "video" AND activeClips[0].videoElement.readyState >= 2 AND activeClips[0].videoElement.videoWidth > 0 AND activeClips[0].videoElement.videoHeight > 0 AND canvasDisplaysBlackInsteadOfVideo()_
    - _Expected_Behavior: For any timeline state where a video clip exists at the playhead position and the video element is loaded with valid dimensions, the canvas displays video content (NOT all black pixels)_
    - _Preservation: Canvas dimension calculation, timeline thumbnails, playhead movement, loading states, error handling, frame caching, VideoPool management, SeekManager logic, multi-track layering must remain unchanged_
    - _Requirements: 1.1, 1.2, 1.3, 2.1, 2.2, 2.3, 3.1, 3.2, 3.3, 3.4, 3.5_

  - [x] 3.2 Implement additional fixes if needed based on root cause analysis
    - If video element drawable state is the issue:
      - Update VideoPool.ts `getVideo()` to ensure video elements have properties that make them drawable to canvas
      - Ensure video elements are not hidden (display:none) and have valid dimensions
      - Consider waiting for `requestVideoFrameCallback()` or similar mechanism after seeking
    - If frame decode timing is the issue:
      - Update CanvasRenderer.tsx `renderFrame()` to add frame decode wait after `seekManager.seekIfNeeded()`
      - Add small delay or wait for frame decode event before calling `renderEngine.renderFrame()`
    - Add enhanced diagnostic logging throughout render pipeline
    - _Bug_Condition: Same as 3.1_
    - _Expected_Behavior: Same as 3.1_
    - _Preservation: Same as 3.1_
    - _Requirements: 1.1, 1.2, 1.3, 2.1, 2.2, 2.3, 3.1, 3.2, 3.3, 3.4, 3.5_

  - [x] 3.3 Verify bug condition exploration test now passes
    - **Property 1: Expected Behavior** - Video Frames Render to Canvas
    - **IMPORTANT**: Re-run the SAME test from task 1 - do NOT write a new test
    - The test from task 1 encodes the expected behavior
    - When this test passes, it confirms the expected behavior is satisfied
    - Run bug condition exploration test from step 1
    - **EXPECTED OUTCOME**: Test PASSES (confirms bug is fixed)
    - Verify canvas output contains non-black pixels representing video content
    - Verify video frames update correctly as playhead moves
    - Verify multiple clips render correctly with proper layering
    - Verify different video aspect ratios render correctly
    - _Requirements: 2.1, 2.2, 2.3_

  - [x] 3.4 Verify preservation tests still pass
    - **Property 2: Preservation** - Non-Video-Rendering Behavior
    - **IMPORTANT**: Re-run the SAME tests from task 2 - do NOT write new tests
    - Run preservation property tests from step 2
    - **EXPECTED OUTCOME**: Tests PASS (confirms no regressions)
    - Confirm all preservation tests still pass after fix:
      - Canvas dimension calculation unchanged
      - Loading state display unchanged
      - Error handling unchanged
      - Frame cache operations unchanged
      - VideoPool management unchanged
      - SeekManager logic unchanged
    - _Requirements: 3.1, 3.2, 3.3, 3.4, 3.5_

- [ ] 4. Checkpoint - Ensure all tests pass
  - Run all tests (bug condition + preservation)
  - Verify bug is fixed: canvas displays video content instead of black rectangle
  - Verify no regressions: all existing functionality preserved
  - If any tests fail, investigate and fix issues
  - Ask the user if questions arise
